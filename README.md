# Форма управления учетными записями

Приложение для создания и управления учетными записями пользователей с поддержкой LDAP и локальных записей. Построено на Vue.js 3 с TypeScript и Vuetify.

## 🚀 Технологический стек

- **Vue.js 3** с Composition API
- **TypeScript** для строгой типизации
- **Vite** для быстрой сборки и разработки
- **Pinia** для управления состоянием с персистентностью
- **Vuetify 3** для Material Design UI компонентов
- **localStorage** для сохранения данных
- **ESLint + Prettier** для качества кода

## 📁 Архитектура проекта

```
src/
├── assets/              # Статические ресурсы
├── components/          # UI компоненты
│   └── AccountManagementForm.vue  # Основная форма
├── stores/              # Pinia store с localStorage
│   └── accounts.ts      # Управление учетными записями
├── plugins/             # Vuetify конфигурация
│   └── vuetify.ts       # Настройка UI библиотеки
├── types/               # TypeScript типы
│   └── index.ts         # Интерфейсы данных
├── App.vue              # Корневой компонент
└── main.ts              # Точка входа
```

## 🛠 Установка и запуск

### Предварительные требования

- Node.js 18+ 
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Сборка для продакшена

```bash
npm run build
```

### Предварительный просмотр сборки

```bash
npm run preview
```

## 📋 Доступные скрипты

- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка для продакшена
- `npm run preview` - Предварительный просмотр сборки
- `npm run lint` - Проверка и исправление кода с помощью ESLint
- `npm run format` - Форматирование кода с помощью Prettier

## 🎯 Функциональность

### Основные возможности

- ✅ **Динамическое добавление** учетных записей
- ✅ **Два типа записей**: LDAP и Локальная
- ✅ **Умная валидация** с отображением ошибок
- ✅ **Автосохранение** в localStorage
- ✅ **Персистентность данных** при перезагрузке
- ✅ **Адаптивный дизайн** для всех устройств
- ✅ **Material Design** интерфейс

### Типы учетных записей

- **LDAP** - внешняя аутентификация, пароль не требуется
- **Локальная** - внутренняя аутентификация, пароль обязателен

### Поля учетной записи

- **Метка** - необязательное поле (до 50 символов)
  - Разделение меток через ";"
  - Преобразование в массив объектов `{text: "метка"}`
- **Тип записи** - выпадающий список (LDAP/Локальная)
- **Логин** - обязательное поле (до 100 символов)
- **Пароль** - обязательное для "Локальная" (до 100 символов)
  - Скрывается для типа "LDAP"
  - Сохраняется как `null` для LDAP

### Логика работы

- **Добавление записи** - кнопка "+" создает новую пустую запись
- **Удаление записи** - кнопка удаления полностью удаляет запись
- **Валидация** - происходит при потере фокуса и изменении значений
- **Автосохранение** - валидные записи сохраняются автоматически
- **Восстановление** - данные загружаются при обновлении страницы

## 🔧 Конфигурация

### TypeScript

Конфигурация TypeScript находится в `tsconfig.json` с поддержкой:
- Строгой типизации
- Path mapping для алиасов (`@/` → `src/`)
- Современного ES2020 синтаксиса

### ESLint + Prettier

- ESLint конфигурация в `.eslintrc.cjs`
- Prettier конфигурация в `.prettierrc`
- Автоматическое исправление кода при коммитах

### Vite

- Конфигурация в `vite.config.ts`
- Поддержка алиасов путей
- Автоматическое открытие браузера
- Source maps для отладки

### Vuetify

- Конфигурация в `src/plugins/vuetify.ts`
- Material Design иконки (MDI)
- Кастомная цветовая схема
- Адаптивная тема

### Хранение данных

- **localStorage** для персистентности
- **Автосохранение** при изменении данных
- **Восстановление** при загрузке приложения
- **Обработка ошибок** при работе с localStorage

## 📱 Адаптивность

Приложение полностью адаптивно и корректно отображается на:
- Десктопных устройствах
- Планшетах
- Мобильных устройствах

## 🎨 UI/UX

- **Material Design** интерфейс с Vuetify
- **Интуитивная навигация** с понятными кнопками
- **Валидация в реальном времени** с подсказками
- **Адаптивный дизайн** для всех устройств
- **Консистентная цветовая схема**

## 🔄 Состояние приложения

Управление состоянием осуществляется через Pinia store:
- **`useAccountsStore`** - управление учетными записями
- **Реактивные данные** с автоматическим обновлением UI
- **localStorage интеграция** для персистентности
- **Автосохранение** при изменении данных
- **Восстановление данных** при загрузке

## 📈 Производительность

- **Оптимизированная сборка** Vite
- **Минимальный bundle size**
- **Быстрая загрузка** приложения
- **Эффективная валидация** без лишних перерендеров
- **Локальное хранение** без серверных запросов

## 🚀 Готовые функции

- ✅ **Добавление учетных записей** с валидацией
- ✅ **Удаление учетных записей** одним кликом
- ✅ **Два типа записей** (LDAP/Локальная)
- ✅ **Автосохранение** в localStorage
- ✅ **Восстановление данных** при перезагрузке
- ✅ **Адаптивный дизайн** для всех устройств

## 📖 Примеры использования

### Создание LDAP записи
1. Нажмите "Добавить учетную запись"
2. Выберите тип "LDAP"
3. Введите логин (например: "john.doe")
4. Поле пароля автоматически скроется
5. Добавьте метки через ";" (например: "работа; админ")

### Создание локальной записи
1. Нажмите "Добавить учетную запись"
2. Выберите тип "Локальная"
3. Введите логин и пароль
4. Добавьте метки при необходимости
5. Данные сохранятся автоматически

### Управление метками
- **Формат**: `метка1; метка2; метка3`
- **Пример**: `важное; срочно; работа`
- **Преобразование**: автоматически в `[{text: "важное"}, {text: "срочно"}, {text: "работа"}]`

## 🚧 Планы развития

- [ ] **Массовые операции** (выбор нескольких записей)
- [ ] **Экспорт данных** (JSON, CSV)
- [ ] **Импорт данных** из файлов
- [ ] **Поиск и фильтрация** записей
- [ ] **Сортировка** по полям
- [ ] **Интеграция с API** для синхронизации
- [ ] **Аутентификация** пользователей
- [ ] **Логирование действий** пользователя

## 📄 Лицензия

MIT License
